const fs = require("fs");
const path = require("path");
// 1.创建文件夹
// const dirname = "./zzy";
// if (!fs.existsSync(dirname)) {
//   fs.mkdir(dirname, (err) => {
//     console.log(err);
//   });
// }

// 2.读取文件夹中的文件
// fs.readdir("./zzy", (err, files) => {
//   console.log(files);
// });

// function getFiles(dirname) {
//   fs.readdir(dirname, { withFileTypes: true }, (err, files) => {
//     files.forEach((file) => {
//       // fs.stat(路径)可以但是麻烦
//       //   console.log(file);
//       if (file.isDirectory()) {
//         const filename = path.resolve(dirname, file.name);
//         getFiles(filename);
//       } else {
//         console.log(file.name);
//       }
//     });
//   });
// }
// getFiles(dirname);

// 3.文件夹的重命名
// fs.rename(dirname, "./code", (err) => {
//   console.log(err);
// });
const fs = require("fs");
const path = require("path");
// 1.创建文件夹
// const dirname = "./zzy";
// if (!fs.existsSync(dirname)) {
//   fs.mkdir(dirname, (err) => {
//     console.log(err);
//   });
// }

// 2.读取文件夹中的文件
// fs.readdir("./zzy", (err, files) => {
//   console.log(files);
// });

// function getFiles(dirname) {
//   fs.readdir(dirname, { withFileTypes: true }, (err, files) => {
//     files.forEach((file) => {
//       // fs.stat(路径)可以但是麻烦
//       //   console.log(file);
//       if (file.isDirectory()) {
//         const filename = path.resolve(dirname, file.name);
//         getFiles(filename);
//       } else {
//         console.log(file.name);
//       }
//     });
//   });
// }
// getFiles(dirname);

// 3.文件夹的重命名
// fs.rename(dirname, "./code", (err) => {
//   console.log(err);
// });
const fs = require("fs");
const path = require("path");
// 1.创建文件夹
// const dirname = "./zzy";
// if (!fs.existsSync(dirname)) {
//   fs.mkdir(dirname, (err) => {
//     console.log(err);
//   });
// }

// 2.读取文件夹中的文件
// fs.readdir("./zzy", (err, files) => {
//   console.log(files);
// });

// function getFiles(dirname) {
//   fs.readdir(dirname, { withFileTypes: true }, (err, files) => {
//     files.forEach((file) => {
//       // fs.stat(路径)可以但是麻烦
//       //   console.log(file);
//       if (file.isDirectory()) {
//         const filename = path.resolve(dirname, file.name);
//         getFiles(filename);
//       } else {
//         console.log(file.name);
//       }
//     });
//   });
// }
// getFiles(dirname);

// 3.文件夹的重命名
// fs.rename(dirname, "./code", (err) => {
//   console.log(err);
// });
const fs = require("fs");
const path = require("path");
// 1.创建文件夹
// const dirname = "./zzy";
// if (!fs.existsSync(dirname)) {
//   fs.mkdir(dirname, (err) => {
//     console.log(err);
//   });
// }

// 2.读取文件夹中的文件
// fs.readdir("./zzy", (err, files) => {
//   console.log(files);
// });

// function getFiles(dirname) {
//   fs.readdir(dirname, { withFileTypes: true }, (err, files) => {
//     files.forEach((file) => {
//       // fs.stat(路径)可以但是麻烦
//       //   console.log(file);
//       if (file.isDirectory()) {
//         const filename = path.resolve(dirname, file.name);
//         getFiles(filename);
//       } else {
//         console.log(file.name);
//       }
//     });
//   });
// }
// getFiles(dirname);

// 3.文件夹的重命名
// fs.rename(dirname, "./code", (err) => {
//   console.log(err);
// });
